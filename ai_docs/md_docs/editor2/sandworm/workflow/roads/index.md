# Adding Roads


Roads are generated by tiling a decal created from a primary *Mesh Static* object along the vector data available.


### See Also


Check this video from the series of [video tutorials on the terrain generation](../../../../videotutorials/essentials/sandworm.md) using Sandworm:


## Preparing a Primary Object


First, we create a *[Mesh Static](../../../../objects/objects/mesh/index.md)* object that will be used as a building block for the generation.


1. Create a plane: click *Create -> Primitive -> Plane* on the Menu bar. ![](create_plane.png)
2. Specify the mesh parameters. For our road, we set the following: ![](plane_params.png) > **Notice:** We recommend using more than a single segment to make road curves smoother.
3. Assign the `UnigineGeoreferencedTerrainGeneration/nodes/road/materials/road.mat` material to the plane. ![](plane_material.jpg) The road is generated by tiling the primary mesh along the axis set by **Forward AXIS** (X, -X, Y, -Y), so make sure that your mesh is oriented correctly � in this case, **Forward AXIS** is *X* (red arrow).
4. [Export](../../../../editor2/exporting_nodes/index.md#export_to_noderef) the plane to a `*.node` file (create a *Node Reference*).


Now that the primary node for roads (the `*.node` asset) is ready for use, you can disable it or delete from the scene.


## Setting the Road Object


1. Create the object: in the *Objects* list of the *Sources* panel, click *+* for *Roads*. ![](add_object.png)
2. In the *Parameters* panel, add a source: click the *Add Source(s)* button, select *Assets* and set the path to the `UnigineGeoreferencedTerrainGeneration/vector/roads.shp` file. As the source is added, we can click the *Preview* button below to see the road data preview on the map. This step is optional, you can create the layer without generating the preview. ![](preview.jpg)
3. Add a [filter](../../../../editor2/sandworm/sources/index.md#filters) to pick specific data from the source and set the following values: ![](residential_road_filter.png) You can either use the drop-downs to select the values or click the ![](../../sources/table_sign.png) button to open the Attributes Table and select the values there. ![](road_attributes_table.png) > **Notice:** This step is optional. You can add no filters at all � in this case all roads available in the data source will be generated.
4. Set the prepared [primary object](#create_primary_object) as *Node* and keep other settings as they are. ![](node_settings.png)
5. Click the *Create Road Object* button.


## Generated Roads


The [generated](../../../../editor2/sandworm/workflow/generate/index.md) roads will look as follows:


![](example.jpg)

*Generated roads*


## What Else


- Read more about the [road parameters](../../../../editor2/sandworm/sources/roads/index.md).
- Read more about the [filter settings](../../../../editor2/sandworm/sources/index.md#filters).
