# Project Files


When you create a new UNIGINE project, you are creating a folder with the same name as your project. This folder includes the following:


![](folder_structure.png)

*Basic structure of the root folder of a UNIGINE project.*


|  |  |  |
|---|---|---|
| ![](folder.png) | `.thumbnails/` | This folder contains asset thumbnails for the [Asset Browser](../../editor2/assets_workflow/index.md#asset_browser). Thumbnails are generated automatically by the Editor as you import new assets. |
|  |  |  |
| ![](folder.png) | `data/` | This folder contains all [assets](../../editor2/assets_workflow/index.md#asset_system) that are used in your project in both native and non-native formats. It is the folder where all work with the project content is performed. |
|  |  |  |
| ![](folder.png) | `bin/` | This folder contains all necessary binary executable files and libraries of the UNIGINE Engine, Editor and plugins as well as binary executable files of your project. |
|  |  |  |
| ![](folder.png) | `data/.cache_textures/` | This folder contains texture cache files � minimized copies generated for all texture runtimes and used by the engine's [data streaming system](../../principles/world_management/index.md#data). These files are generated by the Engine on the fly. So, you shouldn't put the content of your project here. |
|  |  |  |
| ![](folder.png) | `data/.runtimes` | This folder contains files in UNIGINE native formats used by the Engine at run time (such as `.texture` textures, `.mesh` geometry, `.anim` animations, etc.). These files are generated by the [UnigineEditor](../../editor2/index.md) for all assets which are not in the UNIGINE's native format (e.g. `.fbx`, `.obj`, `.hdr`, etc.). So, you shouldn't put the content of your project here. |
|  |  |  |
| ![](folder.png) | `data/.thumbnails` | This folder stores the scene thumbnail used for displaying the project in *SDK Browser*. The thumbnail is generated automatically by UnigineEditor when you save changes. This folder cannot be accessed via the *Asset Browser*. When upgrading the project, the folder is deleted automatically. |
|  |  |  |
| ![](folder.png) | `source/` | This folder stores the source files of your application logic depending on the selected API. These files can be edited in an IDE or any text editor. When changing these files it is required to rebuild the application or its parts. > **Notice:** This folder will not be created for projects using UnigineScript API only. |
|  |  |  |
| ![](file.png) | `*.project` | Project file. It includes all necessary metadata and settings for your UNIGINE project accessible from the [SDK Browser](../../sdk/index.md). |
|  |  |  |
| ![](file.png) | `launch_debug` | Launcher for the *debug* version of your UNIGINE project (`.bat` or `.sh` file). |
| ![](file.png) | `launch_editor` | Launcher for your UNIGINE project with the UnigineEditor loaded (`.bat` or `.sh` file). |
| ![](file.png) | `launch_release` | Launcher for the *release* version of your UNIGINE project (`.bat` or `.sh` file). |


> **Notice:** By default, the final project build includes all files available in the project folder. For the proper functioning, the engine requires only runtime files, such as `*.texture`, `*.mesh`, `*.anim`, `*.node`, etc. Therefore, all imported assets (these are usually `*.fbx` files and source textures) can be deleted.
>  However, note that for some files the asset is a runtime (`*.mat`, `*.prop`, `*.mesh`). These files shall not be deleted.
> The `source/`, `bin/` folders, the `*.project` file and launchers are excluded from the final build by default.


### See Also


- The [Version Control](../../editor2/assets_workflow/version_control/index.md) article to learn which files and folders of your project should be added to version control.
