# Navigation

> **Notice:** The complete sample source code is available on GitHub:
> **[github.com/unigine-engine/cpp-api-samples](https://github.com/unigine-engine/cpp-api-samples)**.


## Navigation Mesh

This sample demonstrates how to configure pathfinding between two points with obstacles using *Navigation Mesh*. The *Route radius* parameter controls the navigation width - increasing it creates wider paths that maintain safe distance from obstacles, while decreasing it allows navigation through tighter spaces.
**SDK Path:***<SAMPLES_PROJECT_PATH>/source/navigation/navigation_mesh*

## Navigation Mesh Demo

This sample demonstrates several material balls collecting coins on a plane with obstacles, navigating around by means of a *Navigation Mesh*. The *Route radius* parameter controls the navigation width - increasing it creates wider paths that maintain safe distance from obstacles, while decreasing it allows navigation through tighter spaces.
**SDK Path:***<SAMPLES_PROJECT_PATH>/source/navigation/navigation_mesh_demo*

## Navigation Sectors

This sample demonstrates how to configure pathfinding between two points with obstacles using *Navigation Sectors*. The *Route radius* parameter controls the navigation width - increasing it creates wider paths that maintain safe distance from obstacles, while decreasing it allows navigation through tighter spaces.
**SDK Path:***<SAMPLES_PROJECT_PATH>/source/navigation/navigation_sectors*

## Navigation Sectors Demo

This sample demonstrates several material balls collecting coins in a cube with obstacles, navigating around by means of a Navigation Sector. The *Route radius* parameter controls the navigation width - increasing it creates wider paths that maintain safe distance from obstacles, while decreasing it allows navigation through tighter spaces.
**SDK Path:***<SAMPLES_PROJECT_PATH>/source/navigation/navigation_sectors_demo*
